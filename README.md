Gold Price Prediction System - Update Package v2.0

วิธีติดตั้ง (เลือก 1 ใน 2 วิธี)

วิธีที่ 1: ติดตั้งอัตโนมัติ (แนะนำ) 

```bash
# 1. ไปยังโฟลเดอร์โปรเจกต์
cd /Users/nichanun/Desktop/DSDN

# 2. คัดลอกไฟล์ทั้งหมดมาที่นี่

# 3. รันสคริปต์ติดตั้ง
bash install_updates.sh
```

### วิธีที่ 2: ติดตั้งด้วยตัวเอง

```bash
# 1. ไปยังโฟลเดอร์โปรเจกต์
cd /Users/nichanun/Desktop/DSDN

# 2. Backup ไฟล์เก่า
mkdir backup_old
cp daily_pipeline.py backup_old/
cp build_feature_store_btc.py backup_old/

# 3. คัดลอกไฟล์ใหม่
cp gold_config.py .
cp predict_gold_skip_sundays.py .

# 4. เสร็จสิ้น!
```

---

## การตั้งค่าครั้งแรก

### 1. เปิดไฟล์ตั้งค่า
```bash
nano gold_config.py
# หรือ
open -e gold_config.py
```

### 2. เลือกประเภทราคาทอง
```python
# ในไฟล์ gold_config.py
GOLD_PRICE_TYPE = "gold_bar_sell" 
# ตัวเลือก:
# "gold_bar_sell"  = ทองแท่ง ราคาขาย (แนะนำ ⭐)
# "gold_bar_buy"   = ทองแท่ง ราคารับซื้อ
# "gold_sell"      = ทองรูปพรรณ ราคาขาย
# "gold_buy"       = ทองรูปพรรณ ราคารับซื้อ
```

**แนะนำ:** ใช้ `gold_bar_sell` เพราะ:
- ไม่มีค่าแรงทำ
- ราคาตามตลาดโลก
- เหมาะสำหรับการวิเคราะห์และทำนาย

---

## ทดสอบว่าติดตั้งสำเร็จ

```bash
# 1. ทดสอบ build feature store
python3 build_feature_store_btc.py

# ต้องเห็นข้อความ:
# [INFO] Using gold price type: gold_bar_sell

# 2. ทดสอบการทำนาย
python3 predict_gold_skip_sundays.py --days 1

# ต้องเห็นข้อความ:
# 📊 Using: gold_bar_sell
# 🔮 Predicting next 1 business days...

# 3. ทดสอบ pipeline (ถ้าไม่ใช่วันอาทิตย์)
python3 daily_pipeline.py
```

---

## 📚 คู่มือการใช้งาน

### สำหรับผู้เริ่มต้น
อ่าน **QUICK_START.md** (ใช้เวลา 5 นาที)
```bash
cat QUICK_START.md
# หรือ
open QUICK_START.md
```

### สำหรับผู้ที่ต้องการรายละเอียด
อ่าน **SYSTEM_UPDATE_SUMMARY.md** (เอกสารฉบับเต็ม)
```bash
cat SYSTEM_UPDATE_SUMMARY.md
# หรือ
open SYSTEM_UPDATE_SUMMARY.md
```

---

## 🎯 การใช้งานหลังติดตั้ง

### รันทุกวันอัตโนมัติ
```bash
# เปิด crontab
crontab -e

# เพิ่มบรรทัดนี้ (รันทุกวัน 17:00 น.)
0 17 * * * cd /Users/nichanun/Desktop/DSDN && /bin/zsh run_daily.sh
```

### ทำนายราคาทอง
```bash
# ทำนาย 1 วัน
python3 predict_gold_skip_sundays.py --days 1 --save

# ทำนาย 7 วันทำการ (ข้ามวันอาทิตย์)
python3 predict_gold_skip_sundays.py --days 7 --save
```

---

## เช็คว่าระบบใช้ราคาทองประเภทไหน

### วิธีที่ 1: ดูจาก config
```bash
cat gold_config.py | grep GOLD_PRICE_TYPE
```

### วิธีที่ 2: ดูจาก log
```bash
python3 build_feature_store_btc.py 2>&1 | grep "Using gold price"
# จะแสดง: [INFO] Using gold price type: gold_bar_sell
```

### วิธีที่ 3: ดูจากผลการทำนาย
```bash
python3 predict_gold_skip_sundays.py --days 1
# จะแสดง: 📊 Using: gold_bar_sell
```

---

##  แก้ไขปัญหา

### ปัญหา: ImportError: gold_config
```bash
# ตรวจสอบว่า gold_config.py อยู่ใน directory ถูกต้อง
ls -la gold_config.py

# ถ้าไม่มี ให้คัดลอกมาใหม่
cp /path/to/gold_config.py .
```

### ปัญหา: Pipeline ไม่รัน
```bash
# ตรวจสอบว่าไม่ใช่วันอาทิตย์
date +%A

# ถ้าเป็นวันอาทิตย์ ระบบจะข้ามอัตโนมัติ
```

### ปัญหา: Feature store ไม่อัพเดท
```bash
# รัน build_feature_store โดยตรง
python3 build_feature_store_btc.py

# ดู error message
python3 build_feature_store_btc.py 2>&1 | grep -i error
```



## เทคนิคแนะนำ

### 1. Backup ข้อมูลทุกสัปดาห์
```bash
cd /Users/nichanun/Desktop/DSDN
tar -czf backup_$(date +%Y%m%d).tar.gz \
  data/Feature_store/ \
  model/ \
  results/
```

### 2. ตรวจสอบ accuracy ของโมเดล
```bash
python3 -c "
import joblib
m = joblib.load('model/model_metadata.pkl')
print(f\"MAE: {m['metrics']['MAE']:.2f} บาท\")
print(f\"RMSE: {m['metrics']['RMSE']:.2f} บาท\")
"
```

### 3. ดูประวัติการทำนาย
```bash
# ดูไฟล์การทำนายทั้งหมด
ls -lt results/predictions_*.csv

# ดูไฟล์ล่าสุด
tail results/predictions_*.csv
```

---

**Version:** 2.0  
**Release Date:** 23 November 2025  
**Compatibility:** Python 3.8+  
**Tested on:** macOS (Intel/Apple Silicon)
