# 🚀 Quick Start Guide - Gold Price Prediction System

## 📌 การตั้งค่าครั้งแรก (5 นาที)

### ขั้นตอนที่ 1: เลือกประเภทราคาทอง
แก้ไขไฟล์ `gold_config.py`:

```python
# เลือก 1 ใน 4 ตัวเลือก:
GOLD_PRICE_TYPE = "gold_bar_sell"   # ✅ แนะนำ - ทองแท่ง ราคาขาย
# GOLD_PRICE_TYPE = "gold_bar_buy"   # ทองแท่ง ราคารับซื้อ
# GOLD_PRICE_TYPE = "gold_sell"      # ทองรูปพรรณ ราคาขาย
# GOLD_PRICE_TYPE = "gold_buy"       # ทองรูปพรรณ ราคารับซื้อ
```

### ขั้นตอนที่ 2: วางไฟล์ในตำแหน่งที่ถูกต้อง
```bash
cd /Users/nichanun/Desktop/DSDN

# วางไฟล์ใหม่ทั้งหมด
- gold_config.py                    # ไฟล์ตั้งค่า
- predict_gold_skip_sundays.py      # สคริปต์ทำนายแบบใหม่
```

### ขั้นตอนที่ 3: ทดสอบระบบ
```bash
# 1. ทดสอบ build feature store
python3 build_feature_store_btc.py

# 2. ทดสอบการทำนาย
python3 predict_gold_skip_sundays.py --days 1

# 3. รัน pipeline เต็ม (ถ้าไม่ใช่วันอาทิตย์)
python3 daily_pipeline.py
```

---

## 📅 การใช้งานประจำวัน

### รันอัตโนมัติทุกวัน (แนะนำ)
```bash
# เปิด crontab
crontab -e

# เพิ่มบรรทัดนี้ (รันทุกวัน 17:00 น.)
0 17 * * * cd /Users/nichanun/Desktop/DSDN && /bin/zsh run_daily.sh
```

### รันด้วยตัวเอง
```bash
cd /Users/nichanun/Desktop/DSDN
python3 daily_pipeline.py
```

**สิ่งที่จะเกิดขึ้น:**
- วันจันทร์-ศุกร์: ดึงข้อมูล → อัพเดท feature store → ทำนาย
- วันเสาร์: ทำทุกอย่าง + train model ใหม่
- วันอาทิตย์: **ข้ามการทำงาน** (ตลาดปิด)

---

## 🔮 การทำนายราคาทอง

### ทำนาย 1 วัน
```bash
python3 predict_gold_skip_sundays.py --days 1 --save
```

### ทำนาย 7 วันทำการ
```bash
python3 predict_gold_skip_sundays.py --days 7 --save
```

**ผลลัพธ์:**
```
📊 Using: gold_bar_sell
🔮 Predicting next 7 business days...
======================================================================
Day 1: 2025-11-24 (Monday) 📈 42,150.00 บาท (+0.85%)
Day 2: 2025-11-25 (Tuesday) 📈 42,280.00 บาท (+0.31%)
...
Day 7: 2025-12-01 (Monday) 📈 42,450.00 บาท (+0.24%)

💾 Predictions saved to: results/predictions_7days_20251123.csv
```

---

## 🎯 สิ่งสำคัญที่ต้องจำ

### 1. ประเภทราคาทอง
| ประเภท | คำอธิบาย | แนะนำ |
|--------|----------|--------|
| `gold_bar_sell` | ทองแท่ง ราคาขาย | ✅ ดีที่สุด |
| `gold_bar_buy` | ทองแท่ง ราคารับซื้อ | ⭐ ดี |
| `gold_sell` | ทองรูปพรรณ ราคาขาย | ⚠️ มีค่าแรง |
| `gold_buy` | ทองรูปพรรณ ราคารับซื้อ | ⚠️ มีค่าแรง |

### 2. วันทำการตลาดทอง
- **เปิด:** จันทร์ - เสาร์
- **ปิด:** วันอาทิตย์

### 3. Schedule การทำงาน
- **ดึงข้อมูล:** ทุกวัน (ยกเว้นอาทิตย์)
- **Train model:** ทุกวันเสาร์
- **ทำนาย:** ทุกวัน (ยกเว้นอาทิตย์)

---

## 🔍 เช็คว่าระบบทำงานถูกต้อง

### ตรวจสอบประเภทราคาที่ใช้
```bash
# วิธีที่ 1: ดู config
cat gold_config.py | grep GOLD_PRICE_TYPE

# วิธีที่ 2: ดู log ของ build_feature_store
python3 build_feature_store_btc.py 2>&1 | grep "Using gold price"
# จะแสดง: [INFO] Using gold price type: gold_bar_sell
```

### ตรวจสอบ Feature Store
```bash
# ดูข้อมูล 5 วันล่าสุด
tail -6 data/Feature_store/feature_store.csv

# หรือใช้ Python
python3 -c "
import pandas as pd
df = pd.read_csv('data/Feature_store/feature_store.csv')
print(df[['date', 'gold']].tail())
"
```

### ตรวจสอบผลการทำนาย
```bash
# ดูไฟล์ทำนายล่าสุด
ls -lt results/predictions_*.csv | head -1

# แสดงเนื้อหา
cat results/predictions_7days_*.csv
```

---

## 🚨 แก้ปัญหาเบื้องต้น

### ปัญหา: Pipeline ไม่รัน
```bash
# เช็ค Python path
which python3

# เช็คว่าอยู่ใน directory ถูกต้อง
pwd
# ต้องได้: /Users/nichanun/Desktop/DSDN
```

### ปัญหา: Import gold_config ไม่ได้
```bash
# ย้าย gold_config.py ไป root
mv gold_config.py /Users/nichanun/Desktop/DSDN/

# หรือเพิ่ม PYTHONPATH ใน run_daily.sh
export PYTHONPATH="/Users/nichanun/Desktop/DSDN:$PYTHONPATH"
```

### ปัญหา: Feature store ไม่อัพเดท
```bash
# รัน manual
python3 ingest_gold.py          # ดึงข้อมูล
python3 build_feature_store_btc.py  # สร้าง feature store

# เช็ค error
python3 build_feature_store_btc.py 2>&1 | grep -i error
```

---

## 📊 ตัวอย่างผลลัพธ์

### ผลการทำนาย 1 วัน
```
🔮 GOLD PRICE PREDICTION
======================================================================

📊 Using: gold_bar_sell
📅 Last available date: 2025-11-23
💰 Last gold price:     41,950.00 บาท

📅 Prediction for:      2025-11-24 (Monday)
💎 Predicted price:     42,150.00 บาท

📈 Change:             +200.00 บาท (+0.48%)

🤖 Model Information:
   Type:        LINEAR
   Features:    30
   MAE:         156.32 บาท
   RMSE:        215.48 บาท
   R²:          0.9876
```

### ไฟล์ CSV ที่บันทึก
```csv
date,day_name,predicted_price,price_type,change,change_pct,is_business_day
2025-11-24,Monday,42150.0,gold_bar_sell,200.0,0.48,True
2025-11-25,Tuesday,42280.0,gold_bar_sell,130.0,0.31,True
2025-11-26,Wednesday,42265.0,gold_bar_sell,-15.0,-0.04,True
...
```

---

## 💡 Tips & Best Practices

### 1. ตั้งค่าที่แนะนำ
```python
# gold_config.py
GOLD_PRICE_TYPE = "gold_bar_sell"  # ทองแท่ง
PREDICTION_DAYS = 7                # ทำนาย 7 วัน
SKIP_CLOSED_DAYS_IN_PREDICTION = True  # ข้ามวันหยุด
```

### 2. Backup ข้อมูลสำคัญ
```bash
# Backup ทุกวันเสาร์
cd /Users/nichanun/Desktop/DSDN
tar -czf backup_$(date +%Y%m%d).tar.gz \
  data/Feature_store/feature_store.csv \
  model/best_model.pkl \
  results/
```

### 3. ตรวจสอบ accuracy
```bash
# ดู metrics ของโมเดล
python3 -c "
import joblib
m = joblib.load('model/model_metadata.pkl')
print(f\"MAE: {m['metrics']['MAE']:.2f} บาท\")
print(f\"RMSE: {m['metrics']['RMSE']:.2f} บาท\")
print(f\"R²: {m['metrics']['R2']:.4f}\")
"
```

---

## 📞 เอกสารเพิ่มเติม

- **เอกสารฉบับเต็ม:** `SYSTEM_UPDATE_SUMMARY.md`
- **ไฟล์ตั้งค่า:** `gold_config.py`
- **สคริปต์หลัก:** `daily_pipeline.py`

---

**Last Updated:** 23 November 2025  
**Version:** 2.0 - With Market Calendar Support
