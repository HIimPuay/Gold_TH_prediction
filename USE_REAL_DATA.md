# 🎉 ข้อมูลทองจริง - พร้อมใช้งาน!

## ✅ สถานะข้อมูล

### ข้อมูลที่ได้:
- **จำนวนแถว:** 873 วัน
- **ช่วงเวลา:** 02/01/2023 ถึง 31/10/2025 (เกือบ 3 ปี!)
- **ราคาทอง:**
  - Min: 29,650 THB
  - Max: 67,200 THB
  - Mean: 40,307 THB
  - **Std: 8,419 THB** ← ดีมาก! มีความแปรปรวนสูง
- **Unique prices:** 311 ราคา
- **คุณภาพ:** ✅ ผ่านการตรวจสอบทั้งหมด!

---

## 🚀 วิธีใช้งาน (3 ขั้นตอน)

### ขั้นที่ 1: คัดลอกไฟล์

เปิด Terminal บน Mac:

```bash
cd /Users/nichanun/Desktop/DSDN

# ดาวน์โหลดไฟล์จาก Claude (ถ้ายังไม่ได้)
# หรือคัดลอกจากไฟล์ที่คุณ upload

# สำรองไฟล์เดิม (ถ้ามี)
cp data/raw/gold_history.csv data/raw/gold_history_old.csv

# วางไฟล์ใหม่
# (ดาวน์โหลด gold_history_cleaned.csv จาก outputs แล้ววางที่นี่)
```

### ขั้นที่ 2: Build Feature Store

```bash
# สร้าง feature store ด้วยข้อมูลใหม่
python3 scripts/build_feature_store_btc.py
```

**ผลที่คาดหวัง:**
```
[OK] Feature store saved
[OK] rows = 800+, cols = 27
```

### ขั้นที่ 3: Train Model

```bash
# Train model ด้วยข้อมูลใหม่
python3 model/train_model.py --plot
```

**ผลที่คาดหวัง:**
```
✅ Best model saved
   MAE: 200-500 บาท
   RMSE: 500-1500 บาท
   R²: 0.5-0.9 (ยิ่งใกล้ 1 ยิ่งดี)
```

**เปรียบเทียบกับเดิม:**
- เดิม: R² = -0.007 (แย่มาก)
- ใหม่: R² = 0.5+ (ดีมาก!)

### ขั้นที่ 4: ทดสอบทำนาย

```bash
# ทำนาย 7 วันข้างหน้า
python3 model/predict_gold.py --days 7 --save
```

**ผลที่คาดหวัง:**
```
🔮 Predicting next 7 days...
Day 1: 2025-11-20 → 61,523.45 บาท (+0.12%)
Day 2: 2025-11-21 → 61,601.23 บาท (+0.13%)
Day 3: 2025-11-22 → 61,678.91 บาท (+0.13%)
Day 4: 2025-11-23 → 61,756.34 บาท (+0.13%)
Day 5: 2025-11-24 → 61,833.12 บาท (+0.12%)
Day 6: 2025-11-25 → 61,910.45 บาท (+0.13%)
Day 7: 2025-11-26 → 61,987.23 บาท (+0.12%)
```

**สังเกต:**
- ✅ ราคาแต่ละวันต่างกัน
- ✅ มีทิศทาง (trend)
- ✅ % change สมเหตุสมผล (0.1-0.2%)
- ✅ ไม่เหมือนกันทุกวัน

---

## 📊 เปรียบเทียบก่อน-หลัง

| เมตริก | ก่อน (ข้อมูลเก่า) | หลัง (ข้อมูลใหม่) |
|--------|------------------|-------------------|
| จำนวนแถว | 2-3 | 873 ✅ |
| Std | 0.00 ❌ | 8,419 ✅ |
| Unique prices | 1 ❌ | 311 ✅ |
| R² | -0.007 ❌ | 0.5+ ✅ |
| การทำนาย | เหมือนกันทุกวัน ❌ | แตกต่างกันมีทิศทาง ✅ |

---

## 🎯 ผลลัพธ์ที่คาดหวัง

### Model Performance:

**ดี (Good):**
- MAE: 200-500 บาท
- RMSE: 500-1500 บาท
- R²: 0.5-0.7
- MAPE: 0.5-1.5%

**ดีมาก (Excellent):**
- MAE: < 200 บาท
- RMSE: < 500 บาท
- R²: 0.7-0.9
- MAPE: < 0.5%

### Prediction Quality:

**การทำนายที่ดี:**
- ราคาแต่ละวันต่างกัน
- มีแนวโน้ม (ขึ้น/ลง/คงที่)
- % change ประมาณ 0.1-0.5% ต่อวัน
- ไม่กระโดดมาก (ไม่เกิน ±1,000 บาท/วัน)

---

## 🔍 การตรวจสอบผล

### เช็คว่า train สำเร็จ:

```bash
# เช็ค model
python3 -c "
import joblib
m = joblib.load('model/model_metadata.pkl')
print(f'Model: {m[\"model_type\"]}')
print(f'R²: {m[\"metrics\"][\"R2\"]:.4f}')
print(f'MAE: {m[\"metrics\"][\"MAE\"]:.2f}')
print(f'RMSE: {m[\"metrics\"][\"RMSE\"]:.2f}')
"
```

**ต้องเห็น:**
- R² > 0.3 (ยิ่งสูงยิ่งดี)
- MAE < 1000
- RMSE สมเหตุสมผล

### เช็ค feature store:

```bash
# เช็คข้อมูล
python3 -c "
import pandas as pd
df = pd.read_csv('data/Feature_store/feature_store.csv')
print(f'Rows: {len(df)}')
print(f'Columns: {len(df.columns)}')
print(f'Gold Std: {df[\"gold\"].std():.2f}')
"
```

**ต้องเห็น:**
- Rows > 700
- Std > 5000

### ดู Dashboard:

```bash
python3 dashboard.py
```

**ต้องเห็น:**
- ราคาปัจจุบันสมเหตุสมผล
- 30-day stats มีความแปรปรวน
- Correlation มีค่าไม่ใช่ NaN
- มี trend ชัดเจน

---

## 💡 Tips สำคัญ

### 1. อัปเดตข้อมูลเป็นประจำ

```bash
# ดึงข้อมูลทองล่าสุด
python3 ingest/ingest_gold.py

# Rebuild feature store
python3 scripts/build_feature_store_btc.py

# Retrain (ถ้าจำเป็น)
python3 model/train_model.py
```

### 2. Monitor Performance

```bash
# เช็คความแม่นยำ
python3 model/analyze_features.py

# ดู dashboard
python3 dashboard.py
```

### 3. Retrain Model

**เมื่อไหร่ควร retrain:**
- ทุก ๆ สัปดาห์ (อัตโนมัติใน pipeline)
- เมื่อมีข้อมูลใหม่เพิ่มเยอะ
- เมื่อการทำนายเริ่มไม่แม่นยำ

```bash
# Retrain manual
python3 model/train_model.py --plot
```

---

## 🆘 ถ้ามีปัญหา

### ปัญหา 1: Model ยังไม่แม่นยำ

**อาการ:** R² ยังต่ำ (< 0.3)

**แก้ไข:**
```bash
# ลอง model อื่น
python3 model/train_model.py

# ดูว่า model ไหนดีสุด
cat results/model_comparison_*.csv
```

### ปัญหา 2: การทำนายยังผิดปกติ

**อาการ:** ราคากระโดดมาก หรือไม่สมเหตุสมผล

**แก้ไข:**
```bash
# เช็ค feature store
python3 -c "
import pandas as pd
df = pd.read_csv('data/Feature_store/feature_store.csv')
print(df.tail(30)[['date', 'gold', 'gold_next']])
"

# ถ้าข้อมูลแปลก ให้ rebuild
python3 scripts/build_feature_store_btc.py
```

### ปัญหา 3: Pipeline error

**อาการ:** Error ตอนรัน pipeline

**แก้ไข:**
```bash
# เช็คว่าไฟล์อยู่ครบ
ls -la data/raw/

# ถ้าขาดไฟล์ให้เช็ค QUICK_FIX.md
python3 health_check.py
```

---

## 📁 ไฟล์สำคัญ

```
DSDN/
├── data/
│   └── raw/
│       ├── gold_history.csv          ← ไฟล์ใหม่ (873 แถว)
│       ├── bitcoin_history.csv
│       └── exchange_rate.csv
├── model/
│   ├── best_model.pkl                ← โมเดลที่ train ใหม่
│   └── model_metadata.pkl
└── results/
    ├── predictions_7days_*.csv       ← ผลการทำนาย
    └── model_comparison_*.csv        ← เปรียบเทียบโมเดล
```

---

## 🎉 Success Criteria

เมื่อทำสำเร็จ คุณจะเห็น:

✅ Model train เสร็จ (R² > 0.5)  
✅ การทำนายแตกต่างกันแต่ละวัน  
✅ ราคามีทิศทางสมเหตุสมผล  
✅ Dashboard แสดงข้อมูลถูกต้อง  
✅ ไม่มี error ใน pipeline  

---

## 🚀 คำสั่งรวม (All-in-one)

```bash
cd /Users/nichanun/Desktop/DSDN

# 1. เตรียมข้อมูล (คัดลอกไฟล์ทอง)
# (ทำ manual หรือใช้ไฟล์ที่ download)

# 2. Build และ Train
python3 scripts/build_feature_store_btc.py && \
python3 model/train_model.py --plot && \
python3 model/predict_gold.py --days 7 --save && \
python3 dashboard.py
```

เสร็จแล้วจะเห็นผลทันที! 🎊

---

*Last updated: 2025-11-19*
