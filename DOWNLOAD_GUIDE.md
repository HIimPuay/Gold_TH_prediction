# 📦 คู่มือดาวน์โหลดและติดตั้ง - Gold Prediction System v2.0

## 📁 ไฟล์ที่ต้องดาวน์โหลด (ทั้งหมด 9 ไฟล์)

### 📚 เอกสาร (5 ไฟล์)
1. **README.md** (11K) - ⭐ อ่านก่อนเลย!
2. **QUICK_START.md** (8.0K) - คู่มือเริ่มต้น 5 นาที
3. **SYSTEM_UPDATE_SUMMARY.md** (14K) - เอกสารฉบับเต็ม
4. **BUGFIX_NOTES.md** (3.9K) - รายละเอียด bug ที่แก้
5. **INSTALLATION_CHECKLIST.md** (6.3K) - Checklist ติดตั้ง

### 🐍 Python Scripts (2 ไฟล์)
6. **gold_config.py** (2.5K) - ⭐⭐⭐ ไฟล์ตั้งค่าหลัก (สำคัญมาก!)
7. **predict_gold_skip_sundays.py** (13K) - สคริปต์ทำนายแบบใหม่
8. **daily_pipeline.py** (3.0K) - Pipeline หลัก (แก้ bug แล้ว)

### 🔧 Scripts (1 ไฟล์)
9. **install_updates.sh** (3.8K) - สคริปต์ติดตั้งอัตโนมัติ

---

## 🚀 วิธีติดตั้ง (3 ขั้นตอน)

### ขั้นตอนที่ 1: ดาวน์โหลดไฟล์ทั้งหมด
```
คลิกดาวน์โหลดไฟล์ทั้ง 9 ไฟล์ด้านบน
บันทึกไว้ที่ Desktop หรือ Downloads
```

### ขั้นตอนที่ 2: คัดลอกไปยังโฟลเดอร์โปรเจกต์
```bash
# เปิด Terminal
cd ~/Desktop/DSDN

# คัดลอกไฟล์ทั้งหมดมาที่นี่
# (ใช้ Finder ลากไฟล์มา หรือใช้คำสั่ง cp)
```

### ขั้นตอนที่ 3: รันสคริปต์ติดตั้ง
```bash
bash install_updates.sh
```

**หรือติดตั้งด้วยตัวเอง:**
```bash
# 1. Backup เก่า
mkdir backup_old
cp daily_pipeline.py backup_old/ 2>/dev/null

# 2. ไม่ต้องทำอะไร ไฟล์พร้อมใช้งานแล้ว!
```

---

## ⚙️ การตั้งค่าครั้งแรก (จำเป็น!)

### เปิดไฟล์ gold_config.py
```bash
nano gold_config.py
# หรือ
open -e gold_config.py
```

### เปลี่ยนบรรทัดนี้:
```python
GOLD_PRICE_TYPE = "gold_bar_sell"  # 👈 เลือกประเภทราคาทอง
```

**ตัวเลือก:**
- `"gold_bar_sell"` - ทองแท่ง ราคาขาย ⭐ **แนะนำ**
- `"gold_bar_buy"` - ทองแท่ง ราคารับซื้อ
- `"gold_sell"` - ทองรูปพรรณ ราคาขาย (มีค่าแรง)
- `"gold_buy"` - ทองรูปพรรณ ราคารับซื้อ (มีค่าแรง)

### บันทึกไฟล์
- **nano:** กด `Ctrl+O` แล้ว `Enter` แล้ว `Ctrl+X`
- **TextEdit:** กด `⌘+S`

---

## ✅ ทดสอบว่าติดตั้งสำเร็จ

### Test 1: ตรวจสอบไฟล์
```bash
cd /Users/nichanun/Desktop/DSDN
ls gold_config.py predict_gold_skip_sundays.py daily_pipeline.py
```
**ต้องเห็นไฟล์ทั้ง 3**

### Test 2: ทดสอบ Config
```bash
python3 -c "from gold_config import GOLD_PRICE_TYPE; print('Config OK:', GOLD_PRICE_TYPE)"
```
**ต้องแสดง:** `Config OK: gold_bar_sell`

### Test 3: ทดสอบ Syntax
```bash
python3 -m py_compile predict_gold_skip_sundays.py
python3 -m py_compile daily_pipeline.py
```
**ต้องไม่มี error**

### Test 4: ทดสอบทำนาย
```bash
python3 predict_gold_skip_sundays.py --days 1
```
**ต้องแสดง:**
```
📊 Using: gold_bar_sell
🔮 Predicting next 1 business days...
...
```

---

## 🎯 คำสั่งที่ใช้บ่อย

### ทำนายราคาทอง
```bash
# ทำนาย 1 วัน
python3 predict_gold_skip_sundays.py --days 1 --save

# ทำนาย 7 วัน (ข้ามวันอาทิตย์)
python3 predict_gold_skip_sundays.py --days 7 --save
```

### รัน Pipeline รายวัน
```bash
python3 daily_pipeline.py
```
*หมายเหตุ: วันอาทิตย์จะข้ามอัตโนมัติ*

### ดูผลลัพธ์
```bash
# ดู feature store ล่าสุด
tail -5 data/Feature_store/feature_store.csv

# ดูการทำนายล่าสุด
ls -lt results/predictions_*.csv | head -1
cat $(ls -t results/predictions_*.csv | head -1)
```

---

## 📖 เอกสารแนะนำให้อ่าน

### สำหรับผู้เริ่มต้น (5-10 นาที)
1. **README.md** - ภาพรวมระบบ
2. **QUICK_START.md** - เริ่มใช้งานทันที

### สำหรับผู้ที่ต้องการรายละเอียด (20-30 นาที)
3. **SYSTEM_UPDATE_SUMMARY.md** - เอกสารฉบับเต็ม
4. **BUGFIX_NOTES.md** - ทำความเข้าใจ bug ที่แก้
5. **INSTALLATION_CHECKLIST.md** - ติดตามความคืบหน้า

---

## 🔍 เช็คว่าระบบทำงานถูกต้อง

### ✅ สัญญาณที่ดี

1. **ไฟล์ครบ:**
```bash
ls gold_config.py predict_gold_skip_sundays.py daily_pipeline.py
# ทั้ง 3 ไฟล์ต้องมี
```

2. **Config ถูกต้อง:**
```bash
cat gold_config.py | grep GOLD_PRICE_TYPE
# แสดง: GOLD_PRICE_TYPE = "gold_bar_sell"
```

3. **ทำนายได้:**
```bash
python3 predict_gold_skip_sundays.py --days 1 | head -3
# แสดง: 📊 Using: gold_bar_sell
```

4. **Feature Store อัพเดท:**
```bash
tail -1 data/Feature_store/feature_store.csv
# ต้องมีข้อมูลล่าสุด
```

### ❌ สัญญาณที่ผิดปกติ

1. **ImportError: gold_config**
```bash
# แก้: ตรวจสอบว่า gold_config.py อยู่ใน directory ถูกต้อง
pwd  # ต้องได้ /Users/nichanun/Desktop/DSDN
ls gold_config.py
```

2. **SyntaxError**
```bash
# แก้: ดาวน์โหลดไฟล์ใหม่อีกครั้ง (เวอร์ชันล่าสุดแก้แล้ว)
```

3. **ทำนายใช้ราคาผิด**
```bash
# แก้: เช็ค gold_config.py
cat gold_config.py | grep GOLD_PRICE_TYPE
# แล้วแก้ไขให้ถูกต้อง
```

---

## 💡 Tips & Tricks

### Tip 1: ตั้งค่า Alias
```bash
# เพิ่มใน ~/.zshrc หรือ ~/.bashrc
alias gold-predict="cd /Users/nichanun/Desktop/DSDN && python3 predict_gold_skip_sundays.py"
alias gold-pipeline="cd /Users/nichanun/Desktop/DSDN && python3 daily_pipeline.py"

# ใช้งาน:
gold-predict --days 7 --save
```

### Tip 2: Schedule รันอัตโนมัติ
```bash
crontab -e

# เพิ่มบรรทัด: รันทุกวัน 17:00 น.
0 17 * * * cd /Users/nichanun/Desktop/DSDN && python3 daily_pipeline.py
```

### Tip 3: Monitor Performance
```bash
# สร้างสคริปต์ quick status
cat > check_status.sh << 'EOF'
#!/bin/bash
echo "📊 Gold Prediction System Status"
echo "=================================="
echo ""
echo "Config:"
grep GOLD_PRICE_TYPE gold_config.py
echo ""
echo "Latest Feature Store:"
tail -1 data/Feature_store/feature_store.csv | cut -d',' -f1-2
echo ""
echo "Model Accuracy:"
python3 -c "import joblib; m=joblib.load('model/model_metadata.pkl'); print(f'MAE: {m[\"metrics\"][\"MAE\"]:.2f} THB')"
EOF

chmod +x check_status.sh
./check_status.sh
```

---

## 🆘 ต้องการความช่วยเหลือ?

### ลำดับการแก้ปัญหา:
1. อ่าน **QUICK_START.md** - มีวิธีแก้ปัญหาพื้นฐาน
2. ดูที่ **BUGFIX_NOTES.md** - เข้าใจ error ทั่วไป
3. ตรวจสอบตาม **INSTALLATION_CHECKLIST.md**
4. อ่าน **SYSTEM_UPDATE_SUMMARY.md** - ส่วน Troubleshooting

### Debug Commands:
```bash
# เช็คว่า Python หา module ได้หรือไม่
python3 -c "import sys; print('\n'.join(sys.path))"

# เช็ค syntax ทั้งหมด
for f in *.py; do python3 -m py_compile $f && echo "✅ $f" || echo "❌ $f"; done

# ดู log ล่าสุด
tail -100 logs/pipeline.log
```

---

## 📊 สรุปความแตกต่าง

### ก่อนอัพเดท
- ❌ ใช้ทองรูปพรรณ (มีค่าแรง)
- ❌ ทำนายวันอาทิตย์ (ตลาดปิด)
- ❌ Pipeline มี bug
- ❌ ไม่สามารถเลือกราคาได้

### หลังอัพเดท
- ✅ เลือกทองแท่งได้ (ไม่มีค่าแรง)
- ✅ ข้ามวันอาทิตย์อัตโนมัติ
- ✅ Pipeline รันได้เสถียร
- ✅ ตั้งค่าได้ง่ายใน config file
- ✅ แสดงชื่อวันในผลลัพธ์

---

## 🎉 เสร็จสิ้น!

เมื่อทำตามขั้นตอนครบแล้ว คุณจะมี:
- ✅ ระบบทำนายราคาทองที่ปรับปรุงแล้ว
- ✅ การเลือกประเภทราคาทองได้ตามต้องการ
- ✅ การข้ามวันอาทิตย์อัตโนมัติ
- ✅ Bug fixes ทั้งหมด
- ✅ เอกสารครบถ้วน

**ระบบพร้อมใช้งาน! 🚀**

---

## 📞 ติดต่อ

หากมีปัญหาหรือข้อสงสัย:
- อ่านเอกสารทั้ง 5 ฉบับ
- ตรวจสอบ error message
- ทดสอบตาม checklist

**Good luck! 🍀**

---

**Version:** 2.0  
**Release Date:** 23 November 2025  
**Total Files:** 9 files (42K total)  
**Installation Time:** ~10 minutes  
**Difficulty:** ⭐⭐☆☆☆ (Easy)
